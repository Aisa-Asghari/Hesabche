<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.shariaty.hesabche">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_hesabche"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_hesabche_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".UOutgoActivity"></activity>
        <activity android:name=".UpdateActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBvYXx1PlveQ5E7pCDEswdityN1JYW71CE" />

        <activity android:name=".DrawerActivity" />
        <activity android:name=".OutgoActivity" />
        <activity android:name=".InviteActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ReportActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IncomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".IntroActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>